const { useDebugValue } = require("react");

console.log("Age is:", age);

var age = 21;

console.log(hello);

console.log("Age is:", age);


function hello() {
    console.log("Hello World!");
}


/*
shallowcopy, deepcopy

shallow clone, deep clone

structuredClone() -- use in nested obj
nested object

be aware while checking something such as typeof null === 'object'

"Hitesh" converting to number gives NaN
"Piyush" converting to number gives NaN
These two NaN are not equal to each other, but they are of type number

String interpolation in js using string literals

What changes occur when we make a variable undefined or null in memory

how do we create placeholer in array

*/